services:
  db:
    image: postgres:16-alpine
    container_name: local_pg_container
    restart: always
    env_file:
      - ./keys/.env
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
